# F1 2026 Prediction Center — Python dependencies
# Tested with Python 3.11. Lower bounds chosen to ensure wheel availability;
# after a successful local install you can run `pip freeze > requirements.txt`
# to lock exact versions.

# --- Web app (runtime) ---
streamlit>=1.32,<2.0
plotly>=5.20,<6.0

# --- Data & numerics (runtime + training) ---
# NumPy lower bound is 1.26 (last 1.x release); upper bound stays open so
# Python 3.13/3.14 environments can pull NumPy 2.x with proper wheels.
pandas>=2.1
numpy>=1.26

# --- Machine learning (runtime: needed to unpickle the trained models) ---
scikit-learn>=1.4,<2.0
xgboost>=2.0,<3.0
joblib>=1.3,<2.0

# --- Data ingestion / model training (only needed when re-running the pipeline) ---
fastf1>=3.3,<4.0
optuna>=3.5,<4.0
tqdm>=4.66,<5.0
