fastf1 ve pandas implemente edebilmek için sanal ortam kurdum.(macbook için geçerli kodlar)

python3 -m venv .venv

source .venv/bin/activate





pip install fastf1 
pip install pandas
pip install tqdm
pip install scikit-learn 
pip install xgboost
pip install optuna
pip install streamlit
pip install plotly


 streamlit run /Users/baran.karluk/F1_project/code/app.py      #uygulamayı runlama kodu